[project]
name = "resume"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "softworker>=1.0.0",
]

[tool.uv.sources]
softworker = { git = "https://github.com/garapatech/softworker.git" }
