[project]
name = "resume"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "antimeta-resume",
]

[tool.uv.sources]
antimeta-resume = { git = "https://github.com/garapatech/antimeta-resume.git" }
